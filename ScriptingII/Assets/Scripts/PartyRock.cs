using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PartyRock : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Guadiamous igitur");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
